h1 Listing users

table
  thead
    tr
      th Object guid
      th Username
      th Ldap attributes
      th
      th
      th

  tbody
    - @users.each do |user|
      tr
        td = user.object_guid
        td = user.username
        td = user.ldap_attributes
        td = link_to 'Show', user
        td = link_to 'Edit', edit_user_path(user)
        td = link_to 'Destroy', user, data: { confirm: 'Are you sure?' }, method: :delete

br

= link_to 'New User', new_user_path
